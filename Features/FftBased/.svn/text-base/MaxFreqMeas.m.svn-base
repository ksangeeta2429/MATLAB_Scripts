% max frequency of excited region 
% use quantile to discount extreme outliers
function Result = MaxFreqMeas(AnomImage,FftFreq, Quant, MinAnom)

    for i=1:size(AnomImage,1)
        Index = find(AnomImage(i,:));
        NumHit = length(Index);

        if (NumHit < MinAnom)
            Out(i) = -1000;
        else
            [Cdf,Order] = sort(FftFreq(Index));
            Out(i) = interp1(Order/NumHit, Cdf, Quant);
        end
    end
    
    Result = max(Out);
end




            
        
